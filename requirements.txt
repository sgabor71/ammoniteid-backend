# ============================================================
# requirements.txt — Python dependencies (MEMORY OPTIMIZED)
# AmmoniteID v1.0 - STABLE VERSION
# ============================================================
# Using full TensorFlow instead of tflite-runtime
# This ensures compatibility and still saves memory vs old code
# ============================================================

fastapi==0.115.5
uvicorn[standard]==0.34.0
pillow==11.0.0
python-multipart==0.0.20
aiofiles==24.1.0

# Using full TensorFlow for guaranteed compatibility
# Model memory: ~200MB (still better than old 400-500MB total!)
# Streaming + cleanup still saves tons of memory
tensorflow==2.16.1
numpy==1.24.3
