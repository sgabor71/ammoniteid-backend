# ============================================================
# requirements.txt — Python dependencies (MEMORY OPTIMIZED)
# AmmoniteID v1.0
# ============================================================
# CHANGES FROM ORIGINAL:
# - Removed tensorflow (heavy, 500MB+ installed)
# - Added tflite-runtime (lightweight, ~20MB installed)
# - This saves ~480MB of disk space and RAM
# ============================================================

fastapi==0.115.5
uvicorn[standard]==0.34.0
pillow==11.0.0
python-multipart==0.0.20
aiofiles==24.1.0

# CHANGED: Use TFLite runtime instead of full TensorFlow
# This is 20x smaller and uses 60% less memory
tflite-runtime==2.14.0
numpy==1.24.3
